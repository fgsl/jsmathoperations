import { multiplicar } from './multiplicar.js';

describe('multiplicaçãp', () => {
  test('multiplica 2 X 3 = 6', () => {
    expect(sum(2, 3)).toBe(6);
  });

  test('multiplica 4 X 5 = 20', () => {
    expect(sum(4, 5)).toBe(20);
  });
});